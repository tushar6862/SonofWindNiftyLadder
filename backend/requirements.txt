Flask==3.0.3
flask-cors==4.0.1
requests==2.32.3
python-socketio==4.6.0
python-engineio==3.13.0
gunicorn>=22.0.0

# Tick engine / dashboard pipeline (optional: redis only if REDIS_URL set)
numpy>=1.26.0
scipy>=1.11.0
pandas>=2.0.0
redis>=5.0.0

# Morning sentiment LLM parse (Moneycontrol → Gemini)
google-genai>=1.0.0

# Fyers LIVE LTP (optional). Prefer quotes poll — full SDK needs aiohttp (broken on Py3.14).
# For WS: pip install path\to\fyers_apiv3-*.whl --no-deps  (and a setuptools that still has pkg_resources)
websocket-client>=1.6.0

